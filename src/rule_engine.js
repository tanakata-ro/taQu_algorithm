'use strict';

// Compatibility entry point. The public implementation is kept in one file so
// it can stay byte-for-byte aligned with the service rule engine.
module.exports = require('./custom_rule.js');
